{
	"name": "server",
	"main": "index.ts",
	"private": true,
	"type": "module",
	"scripts": {
		"destroy": "docker rm pkm_champions_coach-database",
		"dev": "npm run destroy && docker compose --env-file ../.env.development up",
		"start": "docker compose --env-file ../.env.production up"
	}
}
